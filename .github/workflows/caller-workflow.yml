name: caller-workflow

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  compile:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Compile C Code
        uses: MyDummyorg159/custom-actions/compile@v1.0.0
        with:
          c_file: "sample.c"

  static-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Static Code Analysis
        uses: MyDummyorg159/custom-actions/static-analysis@v1.0.0
        with:
          c_file: "sample.c"
